{% extends 'TmtCoreBundle:Template:body.html.twig' %}

{% block title %}{{ 'Projekt'|trans }}{% endblock %}

{% block content %}
    {% for project in projects %}
        <div>
            <a href="{{ path('tmt_project_show', {projectId:project.id}) }}">{{project.name}}</a>
        </div>
    {% endfor %}
{% endblock %}
